<Page
    x:Class="NextGenEmby.App.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource AppBackgroundBrush}">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="72,48" MaxWidth="980" HorizontalAlignment="Left">
                <StackPanel Spacing="24">
                    <TextBlock Text="Search" FontSize="46" FontWeight="SemiBold" />
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox
                            x:Name="SearchBox"
                            Header="Title"
                            PlaceholderText="Search movies, shows, and episodes"
                            UseSystemFocusVisuals="True" />
                        <Button
                            x:Name="SearchActionButton"
                            Grid.Column="1"
                            VerticalAlignment="Bottom"
                            Content="Search"
                            Click="Search_OnClick" />
                    </Grid>
                    <TextBlock
                        x:Name="StatusBlock"
                        FontSize="20"
                        Foreground="{StaticResource AppMutedTextBrush}"
                        Text="Search results will appear here."
                        TextWrapping="Wrap" />
                    <StackPanel x:Name="ResultsPanel" Spacing="14" />
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
