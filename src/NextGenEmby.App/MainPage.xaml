<Page
    x:Class="NextGenEmby.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource AppBackgroundBrush}">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="ShellHeader"
            Grid.Row="0"
            Margin="56,28,56,18"
            RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="28"
                    FontWeight="SemiBold"
                    Text="Next Gen Xbox Emby"
                    TextTrimming="CharacterEllipsis" />

                <Button
                    x:Name="SettingsButton"
                    Grid.Column="1"
                    AutomationProperties.Name="Settings"
                    Click="Settings_OnClick"
                    Style="{StaticResource IconButtonStyle}"
                    ToolTipService.ToolTip="Settings"
                    UseSystemFocusVisuals="True">
                    <SymbolIcon Symbol="Setting" />
                </Button>
            </Grid>

            <ScrollViewer
                Grid.Row="1"
                HorizontalScrollBarVisibility="Hidden"
                HorizontalScrollMode="Enabled"
                VerticalScrollBarVisibility="Disabled"
                VerticalScrollMode="Disabled">
                <StackPanel
                    HorizontalAlignment="Left"
                    Orientation="Horizontal"
                    Spacing="16">
                    <Button x:Name="HomeButton" Content="Home" MinWidth="150" Click="Home_OnClick" />
                    <Button x:Name="MoviesButton" Content="Movies" MinWidth="150" Click="Movies_OnClick" />
                    <Button x:Name="TvButton" Content="TV" MinWidth="150" Click="Tv_OnClick" />
                    <Button x:Name="SearchButton" Content="Search" MinWidth="150" Click="Search_OnClick" />
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Frame
            x:Name="ContentFrame"
            Grid.Row="1"
            Background="{StaticResource AppBackgroundBrush}" />
    </Grid>
</Page>
