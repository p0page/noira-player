<Page
    x:Class="NextGenEmby.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource AppBackgroundBrush}">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            x:Name="ShellHeader"
            Grid.Row="0"
            Background="#E603060A"
            BorderBrush="{StaticResource AppHairlineBrush}"
            BorderThickness="0,0,0,1">
            <Grid
                Margin="{StaticResource TvShellMargin}"
                RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ColumnSpacing="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Border
                            Width="4"
                            Height="28"
                            Background="{StaticResource AppAccentBrush}"
                            CornerRadius="2" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="23"
                            FontWeight="SemiBold"
                            Foreground="{StaticResource AppTextBrush}"
                            Text="Next Gen Xbox Emby"
                            TextTrimming="CharacterEllipsis" />
                    </StackPanel>

                    <Button
                        x:Name="SettingsButton"
                        Grid.Column="1"
                        AutomationProperties.Name="Settings"
                        Click="Settings_OnClick"
                        Style="{StaticResource IconButtonStyle}"
                        ToolTipService.ToolTip="Settings"
                        UseSystemFocusVisuals="True">
                        <SymbolIcon Symbol="Setting" />
                    </Button>
                </Grid>

                <ScrollViewer
                    Grid.Row="1"
                    HorizontalScrollBarVisibility="Hidden"
                    HorizontalScrollMode="Enabled"
                    VerticalScrollBarVisibility="Disabled"
                    VerticalScrollMode="Disabled">
                    <StackPanel
                        HorizontalAlignment="Left"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Button x:Name="HomeButton" Content="Home" Click="Home_OnClick" Style="{StaticResource TvNavButtonStyle}" />
                        <Button x:Name="MoviesButton" Content="Movies" Click="Movies_OnClick" Style="{StaticResource TvNavButtonStyle}" />
                        <Button x:Name="TvButton" Content="TV" Click="Tv_OnClick" Style="{StaticResource TvNavButtonStyle}" />
                        <Button x:Name="SearchButton" Content="Search" Click="Search_OnClick" Style="{StaticResource TvNavButtonStyle}" />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <Frame
            x:Name="ContentFrame"
            Grid.Row="1"
            Background="{StaticResource AppBackgroundBrush}" />
    </Grid>
</Page>
